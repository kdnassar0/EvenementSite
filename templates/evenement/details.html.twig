{% extends 'base.html.twig' %}


{% block body %}

<div class="containerPage1">

    <div class="elementsNav">
        <div class="logo">LOGO</div>
        <nav>
            <ul class="ul1">
                <a href="{{ path ('app_categorie')}}">
                    <li>Accueil</li>
                </a>

                <a href=" {{ path ('app_lieu') }}">
                    <li>Lieu</li>
                </a>
                <a href="{{ path ('add_evenement') }}">
                    <li>Organiser</li>
                </a>
                <a href="{{ path ('app_oragnisateur') }}">
                    <li>Profil</li>
                </a>
                <a href="#">
                    <li>contact</li>
                </a>
            </ul>
        </nav>

        <div class="search">
            <a href="{{ path ('app_admin') }}">
                <li>Admin</li>
            </a>
            <a href="{{ path('app_logout') }}">
                <li>logout</li>
            </a>
        </div>
    </div>


    <div class="titreEvenement">
        <h1>{{ evenement.nom }}</h1>
    </div>
    <div class="elementDetails">
        <div class="description">
            <img src="{{asset ('images/evenement/'~ evenement.image) }}" alt="">
            <div class="descriptionTexte">{{ evenement.description }}</div>
        </div>
        <div class="info">
            <div class="email">référence : {{ createur.email }}</div>
            <div class="date">{{ evenement.dateDebut | date('y-m-d H:i') }}</div>
        </div>

    </div>
    <br>

    {% for commentaire in commentaires %}
    {{ commentaire }} <br>
    <a href="{{ path('supprimer_commentaire',{'id':commentaire.id})}}">supprimer</a>
    {% endfor %}


    {{form_start( formAddCommentaire) }}
    {{form_row( formAddCommentaire.text) }}
    {{form_end( formAddCommentaire) }}



    {% endblock %}