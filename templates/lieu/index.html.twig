{% extends 'base.html.twig' %}

{% block title %}Hello LieuController!{% endblock %}

{% block body %}



{% for lieu in lieus %}
        {{ lieu.nom }}
        {{ lieu.description  }}
        {{ lieu.dateCreation | date('d/m/y')  }}
        {{ lieu.ville }}
        {{ lieu.adress}}
       <img src="{{ lieu.image }}" alt=""> 

       <a href="{{ path ('supprimer_lieu',{'id':lieu.id}) }}">supprimer</a>
       <a href="{{ path ('edit_lieu',{'id':lieu.id}) }}">Edit</a>
    
<div class="containersalle">
    {% for salle in lieu.salles %} 
    <div class="elemantsSalle">  
            {{ salle.numero }}
            {{ salle.capacite }}
            {{ salle.discreption }}
            <img src=" {{ salle.image }}" alt=""><br>
        </div>    
    {% endfor %}
        
{% endfor %} 

{{ form (formAddlieu) }}

</div>











{% endblock %}
